#include <stdio.h>
#include <stdlib.h>

int load_row(char *file) {
}

int main(int argc, char **argv) {
    if (argc != 4) {
        printf("error: numebr of arguments is %d\n", argc);
        exit(-1);
    }
    
}
